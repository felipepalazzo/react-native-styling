import { BOX, COLORS } from '../../variables'

export const button = {
  alignItems: 'center',
  paddingTop: BOX.small,
  paddingBottom: BOX.small,
  borderRadius: 3,
  width: 40,
}

export const theme = {
  main: {
    color: 'lightsalmon'
  },
}
