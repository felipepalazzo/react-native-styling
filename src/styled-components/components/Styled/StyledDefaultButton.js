import { TouchableOpacity } from 'react-native'
import styled from 'styled-components'

export const StyledDefaultButton = styled(TouchableOpacity)`
  border-radius: 3px;
  width: 40px;
  padding-top: 10px;
  padding-bottom: 10px;
`
